version: "3.9"

services:
  postgres:
    image: postgres:16-alpine
    container_name: gql_learn_postgres
    environment:
      POSTGRES_USER: gql_learn
      POSTGRES_PASSWORD: learner123
      POSTGRES_DB: gql_learn
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U gql_learn"]
      interval: 10s
      timeout: 5s
      retries: 5

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: gql_learn_pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@example.com
      PGADMIN_DEFAULT_PASSWORD: admin
    ports:
      - "5050:80"
    depends_on:
      postgres:
        condition: service_healthy

volumes:
  postgres_data:
